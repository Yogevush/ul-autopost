# הוראות פתיחה לקלוד

**קרא את הקובץ `PROJECT.md` שבתיקייה הזאת לפני שאתה עושה משהו.**

הוא מסמך המצב המרכזי של הפרויקט: הכללים המחייבים, הטענות המותרות, המזהים, המערכת האוטומטית והקמפיין הממומן. אל תתחיל לעבוד בלי לקרוא אותו.

**מה הפרויקט כולל.** שני ערוצי פרסום על אותו נכס, שניהם במטא. פרסום אורגני בפייסבוק ובאינסטגרם, מערכת אוטומטית שמפרסמת לבד לפי לוח שידורים. ופרסום ממומן במטא, קמפיין לידים לגיוס מרצים. אותו דף, אותו חשבון אינסטגרם, אותה אפליקציה במטא. שינוי באסימון או בהרשאות משפיע על שניהם.

אם אתה בענן ואין לך את הקובץ, שכפל את המאגר הציבורי וקרא אותו משם:

git clone https://github.com/Yogevush/ul-autopost

## שלושה כללים שלא ממתינים לקריאת המסמך

1. **עברית בלבד, בלי מקף ארוך ובלי מקף בינוני.** כל מילה באנגלית עומדת בשורה נפרדת, עם שורה ריקה לפניה ואחריה.
2. **לא לפרסם, לא להוציא כסף ולא לבצע פעולה בלתי הפיכה בלי אישור מפורש של יוגב.** להכין עד הסוף, ואז לשאול.
3. **אמת בלבד.** רק הטענות שמופיעות בסעיף 3 של המסמך. בלי להמציא מספרים, עדויות או מחקרים.

## איפה נמצא מה

- מצב הפרויקט וכל ההחלטות: `PROJECT.md`
- הסבר טכני על מערכת הפרסום: `README.md`
- מה כבר פורסם: `state/published.json`
- תור התוכן: `content/queue.json`
